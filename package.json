{
  "name": "floware",
  "version": "1.0.0",
  "private": true,
  "description": "Explainable Financial Risk Intelligence Layer & Real-Time Investigation Platform",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
